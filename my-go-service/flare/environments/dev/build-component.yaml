apiVersion: components.flare.engineering/v1alpha1
kind: Component
metadata:
  name: my-go-service-build-dev
spec:
  repository:
    name: my-go-service
  plan:
    name: go-build-plan
  environment:
    name: dev
  inputs:
    workDir: /workspace/checkout/my-go-service/service
